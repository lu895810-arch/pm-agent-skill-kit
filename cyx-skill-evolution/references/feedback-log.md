# 反馈账本 · feedback-log（cyx 技能通用）

> **本账本服务于所有 `cyx-*` 技能**：每个技能被使用后，用户在过程中提的修改意见、补充、纠偏，
> 凡判定为**通用规则**的，都按 `cyx-skill-evolution` 的格式在这里追加一行；规则本体写进对应 skill。
>
> **为什么集中在母版目录**：多数技能目录下没有 `references/`，分散建账会让规则散落、无法对照。
> 已经自带账本的技能包（`cyx-research-report` 三 skill、`cyx-ears-need`、`cyx-summary-need`）**优先写各自那本**，
> 其余技能一律写本文件——**一个技能只有一本账，不要两头都写**。
>
> **追加规则**：一行一次；只记**通用规则**，个案（只对这一份产物成立的结论、数字）不入表；
> 「归属技能」列必填；同类再次出现时在「处置」列标「高频」，并把该条在对应 skill 里从「建议」升为「强约束」。

## 格式

```
| # | 日期 | 用户原话（节选） | 类别 | 归属技能 | 落点（文件 · 章节） | 处置 |
```

类别代号沿用通用分档：A 事实与依据 / B 遣词与命名 / C 视角与元信息 / D 结构与层次 / E 术语与可读性 / F 排版与视觉 / G 流程与命令 / 组织 skill 自身机制。

---

## 日志

| # | 日期 | 用户原话（节选） | 类别 | 归属技能 | 落点 | 处置 |
|---|---|---|---|---|---|---|
| 1 | 2026-09-21 | 「全局的 skill 都具备自我进化机制，对不？我在核查确认下」 | 组织 | 全部 cyx 技能 | 新建 `cyx-skill-evolution` 母版；13 个 cyx 技能各加一节「自我进化机制」并指向母版 | 新增·**机制统一** |
| 2 | 2026-09-23 | 「请你按照实际情况更新上述三份文档」（附 7 张产品实际界面截图） | G | `cyx-doc-align-baseline` | 新增「第 7 步：按新事实回填」——落点表先列全、回填标来源级、顺手查自相矛盾、元叙述跟着改、界面标签优先、不乱升版本号；并记入本机 Edit 连续编辑静默不落盘的落盘陷阱 | 新增 |
| 3 | 2026-09-23 | 「这句话请你解析严谨，它的能力与出海本地化转绘的能力基本是一致的」（圈出总览表里「用户改不了内部细节、可控性最低」一格） | A | `cyx-doc-align-baseline` | 第 7 步规则 3 扩写三条子项：①同一事实按「形态」逐个扫（总览表 / 分档对照表 / 小标题 / 局限框 / 术语表 / 变更记录）；②能力档位定性一改要连带重排分档结构并回扫档数一致；③实战例（阶段2 与阶段3 同档的连带改动面） | 新增 |
| 4 | 2026-09-23 | 「【剧集制作】【出海转绘】【新建空项目】的分镜界面是……可以拖动排序、选中、编辑、复制分镜、删除，下方也有新增一集功能」（附 7 张截图） | G | `cyx-doc-align-baseline` | 第 7 步新增规则 7（回填前先 `Grep` 旧串：Edit 报 String not found 可能是前轮已改、非内容漂移，别当失败重试）、规则 8（跨入口共享能力一处写全、其余处交叉引用，收尾按关键词核对三份命中数） | 新增 |
| 5 | 2026-09-23 | 「这个需要遵循P0在前，P1在后的逻辑」（圈出 3.1 输入与结构化模块 A 的整张需求表） | G | `cyx-doc-align-baseline` + `cyx-summary-need` | 第 7 步新增规则 9（往既有表格补条目要插回它该在的优先级段，不许 `append` 到表尾；改完用脚本抽 `(编号, 优先级)` 序列验单调；编号是稳定标识，不随排序重排）；`cyx-summary-need` 优先级公约与自查补「增量补行同样受排序纪律约束」 | 新增 |
| 6 | 2026-09-23 | 「将该技能命名为：cyx-summary-need」 | 组织 | `cyx-skill-evolution` + `cyx-summary-need` | 母版新增「3.4 技能改名的收口清单」——五处落点（目录 / 本技能文件 / 交叉引用含各技能账本 / 全局记忆含裸词 / 应用选择器缓存）＋两处刻意不改（历史原话与日志）＋文体标签先问别顺手改；`cyx-summary-need` 目录与文件同步更名，其自带账本记一行 | 新增 |

> **本次核查的实测结果**（2026-09-21）：60 个自带 SKILL.md 的技能里，只有 3 个（`cyx-research-report` /
> `cyx-md-style` / `cyx-html-style`，同属一个技能包）有完整进化条款；`cyx-summary-need`、`cyx-ears-need`
> 有两节但各自为政；`cyx-course-html` 等 55 个没有条款——而 `cyx-course-html` 事实上进化最勤（正文里满是
> 「2026-09-15 新增，用户反馈驱动」），只是没写成纪律，也缺账本。本次把它补成机制。
> 第三方模板类技能（`deck-*` / `pdf` / `pptx` / `docx` / `xlsx` 等）**不纳入**——它们是模板库，改了会被覆盖，也不归我们。
| 7 | 2026-09-23 | 「同时将相应的skill技能也同步优化，之后skill具备自主进化机制，根据用户的交互，同步更新其方法论」 | 组织 | `cyx-skill-evolution` + `cyx-summary-need` | 母版 §1 触发时机补「每次交付收尾必回扫」（把机制从被动响应改成自主进化）、§6 交付后自检补对应一项；`cyx-summary-need` 第 7 章新增 7.6 每轮交付的自动回扫（五步：回扫交互 → 判通用性 → 归类 → 回扫四处落点 → 落盘告知）+ 7.2 落点表补母版代号 | 新增 |