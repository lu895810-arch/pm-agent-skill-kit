# 反馈日志 · cyx-original-need-md

> 本文件是 `cyx-original-need-md` 的**进化账本**：用户使用本技能时提的反馈里**可复用的部分**记在这里，
> 对应的规则改动写进 `SKILL.md`「十二、自我进化机制」指向的各节。
>
> **追加规则**：只记**通用规则**，不记个案；一行一次；「归属 skill」列必填；同一类别再次出现时在
> 「处置」列标注「高频」，并把对应 skill 里的该条从「建议」升为「强约束」。个案（只对这一份产品稿
> 成立的结论、数字）不入本表。
>
> 判定、留痕、回退的共性条款见 `cyx-skill-evolution`。

## 分类字典

沿用 `cyx-skill-evolution` 的通用分档：

| 代号 | 类别 | 一句话判据 |
|---|---|---|
| A | 事实与依据 | 结论要有来源、点出是哪份材料 / 哪张截图 |
| B | 遣词与命名 | 用行业口径的词、术语要给白话定义 |
| C | 视角与元信息 | 叙述主体是产品 / 界面 / 用户，不写「我们」「本次调研」与修订流水 |
| D | 结构与层次 | 阶段怎么切、八要素是否同构、表格列是否够 |
| E | 术语与可读性 | 非本行业的人能不能复述 |
| F | 排版与视觉 | 图标、层级、表格宽度（要 HTML 版时转 `cyx-html-style`） |
| G | 流程与命令 | 脚本跑不通、步骤顺序不对 |
| 组织 | skill 自身的拆分 / 改名 / 机制 | 新建、改名、进化条款本身 |

---

## 归属路由（这条反馈该改哪个文件）

| 反馈落在哪 | 本地落点 |
|---|---|
| 骨架、阶段切法、八要素、表格列 | `SKILL.md` 一 / 三 / 四 / 五 ＋ `references/structure-playbook.md` ＋ `assets/template.md` |
| 来源分级与口径冲突 | `SKILL.md` 二 / 七（第 2、6 条） |
| 需求主线、缺口池 | `SKILL.md` 五 / 六 ＋ playbook 五 / 六 |
| 语言与视角 | `SKILL.md` 七 → 实际上游是 `cyx-md-style` 与 `~/.workbuddy/MEMORY.md`「语言总纲」，回写时优先改上游 |
| 机检规则 | `scripts/check_original_need_md.py` ＋ `SKILL.md` 九（自查清单）两处同步 |
| 视觉（配色 / 版式 / 组件） | 转 `cyx-html-style`，本体不动 |
| 下游（要 PRD / 需求文档 / 竞品对比） | 转 `cyx-summary-need` / `cyx-ears-need` / `cyx-research-report`，不在本 skill 里做 |

**边界规则**：一条反馈只有一个主责 skill；跨界的拆成两行记，不合并成一行含糊表述。

---

## 日志

### 2026-09-23 · 技能建立（1 条用户反馈 ＋ 2 条自检发现）

| # | 原话（节选）/ 发现 | 类别 | 归属 skill | 落点 | 处置 |
|---|---|---|---|---|---|
| 1 | 「根据 `LumenFlow功能演进与需求分析.md` 整理为通用的技能 skill，用于单个产品的需求分析处理，技能命名 `cyx-original-need-md`」 | 组织 | cyx-original-need-md | 新建技能包：`SKILL.md`（六段骨架 + 四类来源分级 + 阶段三条判据 + 八要素 + 三张表 + 主线归纳 + 语言纪律 + 六阶段流程 + 自查清单 + 机检 + 下游衔接）/ `assets/template.md`（起手骨架）/ `references/structure-playbook.md`（逐段写法与范例）/ `scripts/check_original_need_md.py`（10 组机检）/ 本账本 | 新建 |
| 2 | 自检发现：源稿正文用 `✅ 满足需求` / `⚠️ 暴露痛点` 当标签 | F | cyx-original-need-md | `SKILL.md` 七（第 5 条）定为硬规则——一律换成加粗文字标签；自查清单同步；脚本组 7 落成断言（emoji 黑名单） | 新增 |
| 3 | 自检发现：源稿缺口池用 `1. 2. 3.` 编号，且不是四列表，「现状证据」没有独立列 | D | cyx-original-need-md | `SKILL.md` 五（需求缺口池）定为 `G-n` 连续编号 ＋ `编号｜缺口｜现状证据｜影响` 四列；与「阶段局限」的区分表同步写入；脚本组 6 落成检查（缺编号 WARN、缺证据列 FAIL） | 新增 |
| 4 | 自检发现：源稿阶段 4 正文没有「产品局限」块（只在总览表里提了），阶段 2 的局限块名写作「暴露严重痛点」 | D | cyx-original-need-md | `SKILL.md` 四（八要素）明确「每阶段同构，缺材料的显式写明『本阶段材料未覆盖』」；playbook 三.3 补同构要求；脚本组 4 按「局限 / 短板 / 天花板 / 不足 / 痛点」多词识别，缺块 FAIL、块名变体 WARN | 新增 |

> **本条性质**：第 1 条是把一份已跑通的产品演进稿反向固化为通用技能；第 2–4 条不是用户反馈，而是建技能时
> 拿源稿回测机检脚本发现的三处体例偏差——它们恰好说明「源稿可作参考实现，但不是合规样本」，脚本对源稿
> 实测 2 FAIL / 7 WARN，对合规样例实测 0 FAIL / 0 WARN。

### 2026-09-23 · 块名一致性机检补齐（姊妹技能比对发现）

| # | 原话（节选）/ 发现 | 类别 | 归属 skill | 落点 | 处置 |
|---|---|---|---|---|---|
| 5 | 建 `cyx-original-need-html`（本 skill 的 HTML 渲染层姊妹技能）时比对发现：本 skill 的 **playbook 三.3 要求「各阶段的块名、块顺序必须完全一致」，但脚本组 4 把「用户需求」与「用户诉求」两种叫法都放行**——规则禁止的，机检不拦，等于这条规则从未真正生效 | D | cyx-original-need-md | `scripts/check_original_need_md.py` 组 4 新增块名收集（`used` 集合）与一致性断言：同一篇里两种叫法并存 → WARN「块名全篇要一致，否则读者会以为是两件事」；`SKILL.md` 十节机检表组 4 描述同步补该项；自查清单不改（规则原文已在 playbook 三.3） | 新增（规则与机检对齐） |

> **本条性质**：不是用户反馈，是两个姊妹技能互查的结果。教训可复用——**同一规则写在 playbook、放行在脚本，
> 是「规则与落点不同步」的隐形变体**：它比模板漂移更难发现，因为没人会怀疑一条「已经检查过」的规则。
> 回扫时除了四文件落点，还要问一句：这条规则在机检里是**拦**、是**放行**、还是**根本没检**。
